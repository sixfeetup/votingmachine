<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      metal:use-macro="base">
    <tal:block metal:fill-slot="content">
        <h1>${context.title}</h1>

        <p>
            From ${context.start} to ${context.end}, ${len(teams)} teams tried to deliver over night.
        </p>

        <p>
            <a href="${request.resource_url(context.__parent__)}">^ up a level</a>
        </p>


        <ul>
            <li tal:condition="teams"><a href="${request.resource_url(context)}/vote">Vote</a></li>
            <li tal:condition="context.results"><a href="${request.resource_url(context)}/results">Results</a></li>
            <li><a href="${request.resource_url(context)}/teams/add">Add new team</a></li>
        </ul>

        <tal:teams condition="teams">
        <h2>Team List</h2>
        <ul>
            <li tal:repeat="team teams">
                ${team.item.title}
            </li>
        </ul>
        </tal:teams>

    </tal:block>
</html>

