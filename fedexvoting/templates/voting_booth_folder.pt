<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      metal:use-macro="base">
    <tal:block metal:fill-slot="content">
        <h1>List of previous fedex days</h1>
        <p><a href="${request.resource_url(context.__parent__)}">^ up a level</a> | <a href="${request.resource_url(context)}/add">Add new FedEx Day</a></p>
        <p>Here is a list of FedEx days newest to oldest</p>
        <ul>
            <li tal:repeat="booth booths">
                <a href="${booth.url}">${booth['item'].title}</a>
            </li>
        </ul>
    </tal:block>
</html>

